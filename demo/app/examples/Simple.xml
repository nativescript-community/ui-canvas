<Page xmlns="http://schemas.nativescript.org/tns.xsd" xmlns:c="@nativescript-community/ui-canvas" navigatedTo="onNavigatedTo">
    <ActionBar title="{{ title }}">
        <NavigationButton text="Back" android.systemIcon="ic_menu_back" tap="onNavigationButtonTap"/>
    </ActionBar>
    <ScrollView>
        <StackLayout>
        	<c:CanvasView width="100" height="100" draw="onDraw"/>
            <Label text="test" fontSize="16" width="100" height="100" backgroundColor="yellow">test</Label>
        </StackLayout>
    </ScrollView>
</Page>
